type Query {
    hello: String
}

type UserToken {
    user: User
    token: String
    error: MutationValidationError
}

scalar Date

type Mutation {
    login(email: String!, password: String!): UserToken
    register(firstName: String!, lastName: String!, dob: Date!, email: String!, password: String!): UserToken
}
